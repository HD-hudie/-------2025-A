# 项目级工作规范

## 建模与数值求解

- 从物理过程推导决策变量，使变量直接对应关键机理；协同问题优先建模事件时刻、空间位置及其衔接关系，避免使用强耦合的间接变量。
- 采用天然满足约束的参数化，确保投放顺序、最小间隔、时间范围和物理可行性在变量生成时成立，避免事后简单截断造成搜索空间畸变和候选重复。
- 全局搜索只用于发现候选解；保留多个独立候选，并使用同一个高精度评分器统一复排和精修。
- 逐步减小时间步长、加密几何采样，检查目标值和策略是否稳定；优化阶段与最终验证阶段必须使用一致的物理判据。
- 使用多组独立随机种子重复求解；若结果差异明显，应扩大搜索或改进参数化，不得挑选单次运行结果作为结论。
- 未经理论证明或充分收敛验证，只能表述为“算法找到的最佳可行解”，不得直接声称为全局最优解。
- 不得将题目评阅区间或预期答案反向写入目标函数、约束、初始种子或停止条件；结果必须由数学模型和算法正向推导得到。

## 实现与验证

- 编写或修改求解代码前，先明确变量定义、约束、目标函数、评分精度和成功判据。
- 对候选策略进行独立高精度复核，并检查投放顺序、爆炸时刻、物理边界及多枚干扰弹的协同增益。
- 区分“可行解”“局部最优解”和“全局最优解”，在运行结果和报告中准确说明证据范围。
